pipeline {
  agent {
    docker { image 'ubuntu' }
  }
  stages {
    stage("Test"){
      steps {
        sh """
        cat /etc/os-release
        ls /
        hostname
        """
      }
    }
  }
}
