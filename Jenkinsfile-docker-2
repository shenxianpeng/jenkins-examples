pipeline {
  agent {
    docker { image 'ubuntu' }
  }
  stages {
    stage("Test"){
      steps {
        sh '''
        echo $SHELL
        cat /etc/os-release
        ls /
        hostname
        '''
      }
    }
  }
}
