pipeline {
  agent {
    docker { image 'ubuntu' }
  }
  stages {
    stage("Test"){
      steps {
         sh """#!/bin/sh
          cat /etc/os-release
          hostname
          ls /
          """
      }
    }
  }
}
