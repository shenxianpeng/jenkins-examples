pipeline {
  agent {
      node {
          label 'master'
      }
  }
  stages {
    stage("Test"){
      steps {
        def img = docker.image("ubuntu")
        img.inside(){
          sh "cat /etc/os-release"
        }
      }
    }
  }
}
